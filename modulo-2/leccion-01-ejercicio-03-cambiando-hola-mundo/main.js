"use strict";

//Selects the element which has a class named "title" and assigns it to a variable
const titleElement = document.querySelector(".title");
//adds a new value to its html syntax
titleElement.innerHTML = titleElement.innerHTML + " mundo";
