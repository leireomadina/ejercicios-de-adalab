"use strict";

const adalaber1 = {
  name: "Susana",
  age: 34,
  job: "periodista",
};

const description = `Mi nombre es ${adalaber1.name}, tengo ${adalaber1.age} años y soy ${adalaber1.job}`;
console.log(description);

const adalaber2 = {
  name: "Rocío",
  age: 25,
  job: "actriz",
};

const description2 = `${adalaber2.name}, ${adalaber2.age} años, ${adalaber2.job}`;
console.log(description2);
